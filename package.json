{
  "name": "overlay-1",
  "version": "2.0.0",
  "description": "Twitch streaming overlay with chat, alerts, and notifications",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "css:watch": "npx tailwindcss -i ./style.css -o ./output.css --watch",
    "css:build": "npx tailwindcss -i ./style.css -o ./output.css --minify"
  },
  "keywords": ["twitch", "overlay", "obs", "stream"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "canvas-confetti": "^1.6.0"
  }
}
